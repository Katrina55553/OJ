# 第 6 章 后端基础架构

## 本章目标

- 理解 Spring Boot 项目的分层架构
- 掌握 MyBatis-Plus 的配置与使用
- 理解 JWT 无状态认证的完整流程
- 学会 AOP 拦截器的实现方式
- 了解 Redis 限流的设计

---

## 6.1 项目结构与分层

### 分层架构

```
请求 → Controller → Service → Manager → Mapper → 数据库
         │            │          │         │
         │            │          │         └── 数据访问（MyBatis-Plus）
         │            │          └── 可复用业务能力封装
         │            └── 核心业务逻辑
         └── REST 接口（参数校验、响应封装）
```

| 层 | 职责 | 示例 |
|---|------|------|
| **Controller** | REST 接口，参数校验，调用 Service | `UserController.userLogin()` |
| **Service** | 核心业务逻辑 | `UserService.login()` |
| **Manager** | 可复用的业务能力封装 | `Manager.executeCode()` |
| **Mapper** | 数据库访问（MyBatis-Plus） | `UserMapper.selectById()` |
| **Model** | 数据模型 | Entity / DTO / VO / Enum |

### Model 子包

| 包 | 用途 | 示例 |
|---|------|------|
| `entity` | 数据库实体，与表一一对应 | `User.java` |
| `dto` | 请求参数对象（前端 → 后端） | `UserLoginRequest.java` |
| `vo` | 响应视图对象（后端 → 前端，脱敏） | `UserVO.java` |
| `enums` | 业务枚举 | `UserRoleEnum.java` |

---

## 6.2 MyBatis-Plus 配置

### 依赖

```xml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-boot-starter</artifactId>
    <version>3.5.3</version>
</dependency>
```

### application.yml 配置

```yaml
mybatis-plus:
  configuration:
    # 关闭驼峰映射（数据库字段已是驼峰命名）
    map-underscore-to-camel-case: false
  global-config:
    db-config:
      # 逻辑删除字段
      logic-delete-field: isDelete
      logic-delete-value: 1
      logic-not-delete-value: 0
      # 自增主键
      id-type: auto
```

### 分页插件

```java
@Configuration
public class MyBatisPlusConfig {
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        interceptor.addInnerInterceptor(
            new PaginationInnerInterceptor(DbType.MYSQL)
        );
        return interceptor;
    }
}
```

### BaseMapper 使用

```java
// UserMapper 继承 BaseMapper，自动获得 CRUD 方法
@Mapper
public interface UserMapper extends BaseMapper<User> {
    // 无需手写 SQL，MyBatis-Plus 提供：
    // selectById(id)          - 按 ID 查询
    // selectList(wrapper)     - 条件查询
    // insert(entity)          - 插入
    // updateById(entity)      - 按 ID 更新
    // deleteById(id)          - 按 ID 删除（逻辑删除）
    // selectPage(page, wrapper) - 分页查询
}
```

---

## 6.3 统一响应格式

### BaseResponse

```java
@Data
public class BaseResponse<T> {
    private int code;       // 状态码，0 表示成功
    private T data;         // 业务数据
    private String message; // 提示信息

    public BaseResponse(int code, T data, String message) {
        this.code = code;
        this.data = data;
        this.message = message;
    }
}
```

### ErrorCode 枚举

```java
public enum ErrorCode {
    SUCCESS(0, "ok"),
    PARAMS_ERROR(40000, "请求参数错误"),
    NOT_LOGIN_ERROR(40100, "未登录"),
    NO_AUTH_ERROR(40300, "无权限"),
    NOT_FOUND_ERROR(40400, "请求数据不存在"),
    SYSTEM_ERROR(50000, "系统内部异常");

    private final int code;
    private final String message;
}
```

### ResultUtils 工具类

```java
public class ResultUtils {
    // 成功响应
    public static <T> BaseResponse<T> success(T data) {
        return new BaseResponse<>(0, data, "ok");
    }

    // 错误响应
    public static BaseResponse<?> error(ErrorCode errorCode) {
        return new BaseResponse<>(errorCode.getCode(), null, errorCode.getMessage());
    }

    public static BaseResponse<?> error(int code, String message) {
        return new BaseResponse<>(code, null, message);
    }
}
```

### 全局异常处理

```java
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    // 业务异常
    @ExceptionHandler(BusinessException.class)
    public BaseResponse<?> businessExceptionHandler(BusinessException e) {
        log.error("BusinessException: {}", e.getMessage());
        return ResultUtils.error(e.getCode(), e.getMessage());
    }

    // 参数校验异常
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public BaseResponse<?> validExceptionHandler(MethodArgumentNotValidException e) {
        return ResultUtils.error(ErrorCode.PARAMS_ERROR);
    }

    // 系统异常
    @ExceptionHandler(Exception.class)
    public BaseResponse<?> exceptionHandler(Exception e) {
        log.error("Exception: ", e);
        return ResultUtils.error(ErrorCode.SYSTEM_ERROR);
    }
}
```

---

## 6.4 JWT 认证流程

### JWT 基础

**JWT（JSON Web Token）** 是一种无状态的认证方案，由三部分组成：

```
Header.Payload.Signature

Header:    {"alg": "HS256", "typ": "JWT"}  → Base64 编码
Payload:   {"userId": 1, "exp": 1700000000} → Base64 编码
Signature: HMAC-SHA256(header + payload, secret) → 签名
```

本项目配置：
- **算法**：HS256（对称加密）
- **过期时间**：604800000ms = **7 天**
- **密钥**：通过 `JWT_SECRET` 环境变量注入

### 登录流程

```java
// UserController.java
@PostMapping("/login")
public BaseResponse<LoginUserVO> userLogin(@RequestBody UserLoginRequest request,
                                            HttpServletRequest httpRequest) {
    // 1. 参数校验
    if (request == null || StringUtils.isBlank(request.getUserAccount())
        || StringUtils.isBlank(request.getUserPassword())) {
        return ResultUtils.error(ErrorCode.PARAMS_ERROR);
    }

    // 2. 调用 Service 登录
    LoginUserVO loginUserVO = userService.userLogin(request, httpRequest);
    return ResultUtils.success(loginUserVO);
}

// UserServiceImpl.java
@Override
public LoginUserVO userLogin(UserLoginRequest request, HttpServletRequest httpRequest) {
    String account = request.getUserAccount();
    String password = request.getUserPassword();

    // 1. 查询用户
    QueryWrapper<User> queryWrapper = new QueryWrapper<>();
    queryWrapper.eq("userAccount", account);
    User user = userMapper.selectOne(queryWrapper);
    if (user == null) {
        throw new BusinessException(ErrorCode.PARAMS_ERROR, "账号不存在");
    }

    // 2. 校验密码（MD5 加密后比对）
    if (!DigestUtils.md5DigestAsHex(password.getBytes()).equals(user.getUserPassword())) {
        throw new BusinessException(ErrorCode.PARAMS_ERROR, "密码错误");
    }

    // 3. 生成 JWT
    String token = JwtUtils.createToken(user.getId());

    // 4. 返回脱敏用户信息
    return getSafetyUser(user, token);
}
```

### JWT 工具类

```java
public class JwtUtils {
    @Value("${jwt.secret}")
    private static String secret;

    @Value("${jwt.expiration}")
    private static Long expiration; // 604800000ms = 7 天

    // 生成 Token
    public static String createToken(Long userId) {
        return JWT.create()
            .withClaim("userId", userId)
            .withExpiresAt(new Date(System.currentTimeMillis() + expiration))
            .sign(Algorithm.HMAC256(secret));
    }

    // 解析 Token
    public static Long parseToken(String token) {
        DecodedJWT jwt = JWT.require(Algorithm.HMAC256(secret))
            .build()
            .verify(token);
        return jwt.getClaim("userId").asLong();
    }
}
```

### JwtInterceptor

```java
@Component
public class JwtInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request,
                             HttpServletResponse response,
                             Object handler) {
        // 1. 获取 Authorization 头
        String token = request.getHeader("Authorization");
        if (StringUtils.isBlank(token) || !token.startsWith("Bearer ")) {
            throw new BusinessException(ErrorCode.NOT_LOGIN_ERROR);
        }
        token = token.substring(7); // 去掉 "Bearer " 前缀

        // 2. 解析 Token
        Long userId;
        try {
            userId = JwtUtils.parseToken(token);
        } catch (JWTVerificationException e) {
            throw new BusinessException(ErrorCode.NOT_LOGIN_ERROR, "Token 无效或已过期");
        }

        // 3. 查询用户信息（优先从 Redis 缓存）
        String cacheKey = "user:id:" + userId;
        User user = (User) redisTemplate.opsForValue().get(cacheKey);
        if (user == null) {
            user = userMapper.selectById(userId);
            if (user != null) {
                redisTemplate.opsForValue().set(cacheKey, user, 30, TimeUnit.MINUTES);
            }
        }
        if (user == null) {
            throw new BusinessException(ErrorCode.NOT_LOGIN_ERROR);
        }

        // 4. 设置用户上下文到 Request 属性
        request.setAttribute("loginUser", user);

        return true;
    }
}
```

### 排除路径

```java
// WebMvcInterceptorConfig.java
@Override
public void addInterceptors(InterceptorRegistry registry) {
    registry.addInterceptor(jwtInterceptor)
        .addPathPatterns("/**")
        .excludePathPatterns(
            "/user/login",      // 登录
            "/user/register",   // 注册
            "/doc.html",        // Knife4j 文档
            "/webjars/**",      // Knife4j 静态资源
            "/v2/api-docs"      // OpenAPI 规范
        );
}
```

---

## 6.5 权限校验（AOP）

### @AuthCheck 注解

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface AuthCheck {
    String mustRole() default "";  // 必须具有的角色
}
```

### AuthInterceptor（AOP 切面）

```java
@Aspect
@Component
public class AuthInterceptor {

    @Around("@annotation(authCheck)")
    public Object doInterceptor(ProceedingJoinPoint joinPoint,
                                 AuthCheck authCheck) throws Throwable {
        // 1. 获取当前用户
        HttpServletRequest request = ((ServletRequestAttributes)
            RequestContextHolder.getRequestAttributes()).getRequest();
        User loginUser = (User) request.getAttribute("loginUser");

        // 2. 获取注解要求的角色
        String mustRole = authCheck.mustRole();
        UserRoleEnum mustRoleEnum = UserRoleEnum.getEnumByValue(mustRole);

        // 3. 如果不需要特定角色，放行
        if (mustRoleEnum == null) {
            return joinPoint.proceed();
        }

        // 4. 获取用户角色
        UserRoleEnum userRoleEnum = UserRoleEnum.getEnumByValue(loginUser.getUserRole());
        if (userRoleEnum == null) {
            throw new BusinessException(ErrorCode.NO_AUTH_ERROR);
        }

        // 5. 管理员可以访问所有接口
        if (userRoleEnum.equals(UserRoleEnum.ADMIN)) {
            return joinPoint.proceed();
        }

        // 6. 角色不匹配，拒绝
        if (!userRoleEnum.equals(mustRoleEnum)) {
            throw new BusinessException(ErrorCode.NO_AUTH_ERROR);
        }

        return joinPoint.proceed();
    }
}
```

### 使用示例

```java
// 只有管理员可以添加题目
@PostMapping("/add")
@AuthCheck(mustRole = "admin")
public BaseResponse<Long> addQuestion(@RequestBody QuestionAddRequest request) {
    // ...
}

// 只有管理员可以删除题目
@PostMapping("/delete")
@AuthCheck(mustRole = "admin")
public BaseResponse<Boolean> deleteQuestion(@RequestBody DeleteRequest request) {
    // ...
}
```

### 三种角色

| 角色 | 值 | 权限 |
|------|-----|------|
| `USER` | `"user"` | 普通用户：刷题、提交、查看记录 |
| `ADMIN` | `"admin"` | 管理员：题目管理、用户管理、全部权限 |
| `BAN` | `"ban"` | 封禁用户：无法登录或使用系统 |

---

## 6.6 接口限流

### @RateLimit 注解

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimit {
    int count() default 10;      // 时间窗口内允许的请求数
    int time() default 60;       // 时间窗口（秒）
}
```

### RateLimitInterceptor

```java
@Component
public class RateLimitInterceptor implements HandlerInterceptor {

    @Autowired
    private RedisTemplate<String, Integer> redisTemplate;

    @Override
    public boolean preHandle(HttpServletRequest request,
                             HttpServletResponse response,
                             Object handler) {
        // 1. 获取方法上的 @RateLimit 注解
        if (!(handler instanceof HandlerMethod)) return true;
        HandlerMethod handlerMethod = (HandlerMethod) handler;
        RateLimit rateLimit = handlerMethod.getMethodAnnotation(RateLimit.class);
        if (rateLimit == null) return true;

        // 2. 构建限流 key（IP + 接口路径）
        String ip = request.getRemoteAddr();
        String uri = request.getRequestURI();
        String key = "rate_limit:" + ip + ":" + uri;

        // 3. Redis 滑动窗口计数
        Long count = redisTemplate.opsForValue().increment(key);
        if (count == 1) {
            // 首次请求，设置过期时间
            redisTemplate.expire(key, rateLimit.time(), TimeUnit.SECONDS);
        }

        // 4. 超过限制，拒绝请求
        if (count > rateLimit.count()) {
            throw new BusinessException(ErrorCode.SYSTEM_ERROR, "请求过于频繁");
        }

        return true;
    }
}
```

### 使用示例

```java
@PostMapping("/login")
@RateLimit(count = 5, time = 60)  // 每分钟最多 5 次登录尝试
public BaseResponse<LoginUserVO> userLogin(@RequestBody UserLoginRequest request) {
    // ...
}
```

---

## 6.7 日志拦截器

```java
@Component
public class LogInterceptor implements HandlerInterceptor {

    private static final Logger log = LoggerFactory.getLogger(LogInterceptor.class);
    private final ThreadLocal<Long> startTime = new ThreadLocal<>();

    @Override
    public boolean preHandle(HttpServletRequest request, ...) {
        startTime.set(System.currentTimeMillis());
        log.info("Request: {} {} from {}",
            request.getMethod(),
            request.getRequestURI(),
            request.getRemoteAddr());
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, ...) {
        long cost = System.currentTimeMillis() - startTime.get();
        log.info("Response: {} {} cost {}ms",
            request.getMethod(),
            request.getRequestURI(),
            cost);
        startTime.remove(); // 防止内存泄漏
    }
}
```

---

## 6.8 CORS 配置

```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
            .allowedOriginPatterns("*")
            .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
            .allowedHeaders("*")
            .allowCredentials(true)
            .maxAge(3600);
    }
}
```

---

## 6.9 小结

| 要点 | 内容 |
|------|------|
| **分层架构** | Controller → Service → Manager → Mapper，职责清晰 |
| **MyBatis-Plus** | 自动 CRUD、分页插件、逻辑删除 |
| **统一响应** | BaseResponse + ErrorCode + ResultUtils + 全局异常处理 |
| **JWT 认证** | 无状态、7 天过期、Redis 缓存用户信息 |
| **AOP 权限** | @AuthCheck 注解 + 切面拦截，三级角色 |
| **接口限流** | @RateLimit + Redis 滑动窗口 |
| **日志拦截** | 请求方法、路径、IP、耗时记录 |

> **下一章预告**：我们将深入后端核心业务——用户系统、题目管理和代码执行的实现。
